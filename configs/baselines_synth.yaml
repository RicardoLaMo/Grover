name: baselines_synth_smoke
baselines:
  - gat
  - single_view_knn
  - single_view_time
  - multi_view_uniform
  - router_no_alignment
train:
  seed: 42
  deterministic: true
  epochs: 10
  lr: 0.001
